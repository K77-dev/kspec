# Tarefa 10.0: Estender SKILL `kspec-bootstrap` para multiplataforma + MCP opt-in

<critical>Ler os arquivos de prd.md e techspec.md desta pasta, se você não ler esses arquivos sua tarefa será invalidada</critical>

## Dependências

- 1.0

## Estimativa

- **Tamanho**: M
- **Horas estimadas**: 2-3h

## Visão Geral

Atualizar `.agents/skills/kspec-bootstrap/SKILL.md` para fluxo multiplataforma. A skill pergunta ao usuário quais plataformas configurar (Claude/Codex/Ambas) e oferece opt-in para registrar MCPs (context7, testsprite) em `.codex/config.toml`. Conforme as respostas, gera os artefatos correspondentes (`CLAUDE.bootstrap.md`, `AGENTS.bootstrap.md` ou ambos). Aborta com mensagem clara em `codex exec`.

<skills>
### Conformidade com Skills Padrões

- `.claude/rules/code-standards.md` — N/A (conteúdo Markdown de skill).
</skills>

<requirements>
- RF5.1 — Bootstrap pergunta "Quais plataformas devem ser configuradas?" com opções "Claude Code apenas", "Codex CLI apenas", "Ambas (Recomendado)" via `AskUserQuestion`.
- RF5.2 — Gera `CLAUDE.bootstrap.md`, `AGENTS.bootstrap.md` ou ambos conforme resposta.
- RF5.3 — Se Codex selecionado, pergunta "Registrar MCP servers (context7, testsprite) em `.codex/config.toml`?" com default **Não**.
- RF5.4 — Quando MCP aceito: criar `.codex/config.toml` com `[mcp_servers.context7]` e `[mcp_servers.testsprite]` com comandos `npx -y` padrão.
- RF5.5 — Em `codex exec` (não-interativo): documentar limitação e abortar com mensagem clara explicando que `AskUserQuestion` não funciona nesse modo.
- Manter compatibilidade com o fluxo legado (gerar `CLAUDE.bootstrap.md` quando o usuário escolhe Claude-only).
</requirements>

## Subtarefas

- [ ] 10.1 Editar `.agents/skills/kspec-bootstrap/SKILL.md`: adicionar etapa "Escolha de plataformas" com prompt via `AskUserQuestion`.
- [ ] 10.2 Adicionar etapa "MCP opt-in" condicional ao Codex; especificar conteúdo do `.codex/config.toml` (template TOML inline).
- [ ] 10.3 Adicionar seção "Limitação em codex exec" no topo da skill, com texto de erro e exit code 1.
- [ ] 10.4 Atualizar a tabela de geração: matriz "plataforma escolhida → arquivos gerados".
- [ ] 10.5 Validação manual: invocar `/kspec-bootstrap` no Claude Code e validar os 3 fluxos (Claude-only, Codex-only, Ambas).

## Detalhes de Implementação

Ver `techspec.md` §Componentes de conteúdo > `.agents/skills/kspec-bootstrap/SKILL.md` e PRD §F5 (Bootstrap multiplataforma) para RFs detalhadas.

## Critérios de Sucesso

- Skill com `AskUserQuestion` para plataforma (RF5.1)
- Skill com `AskUserQuestion` para MCP quando Codex (RF5.3)
- Template TOML para `.codex/config.toml` documentado in-line
- Mensagem de abort em `codex exec` documentada

## Testes da Tarefa

- [ ] Validação manual: 3 fluxos (Claude-only, Codex-only, Ambas)
- [ ] Validação manual: MCP=Sim gera `.codex/config.toml` correto
- [ ] Validação manual: MCP=Não não cria o arquivo

<critical>SEMPRE CRIE E EXECUTE OS TESTES DA TAREFA ANTES DE CONSIDERÁ-LA FINALIZADA</critical>

## Arquivos relevantes

- `.agents/skills/kspec-bootstrap/SKILL.md` (modificado)
