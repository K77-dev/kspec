# Tarefa 2.0: Criar `AGENTS.md` raiz, fixtures `.codex/agents/*.toml` e symlinks `.codex/skills/`

<critical>Ler os arquivos de prd.md e techspec.md desta pasta, se você não ler esses arquivos sua tarefa será invalidada</critical>

## Dependências

- 1.0

## Estimativa

- **Tamanho**: M
- **Horas estimadas**: 2-4h

## Visão Geral

Entregar a camada Codex CLI mínima para o PR-1: arquivo `AGENTS.md` na raiz (equivalente do `CLAUDE.md`), 3 arquivos `.codex/agents/<nome>.toml` gerados manualmente como fixture (antes de existir o gerador automático da Task 6.0/8.0), e symlinks `.codex/skills/<nome>` apontando para `.agents/skills/<nome>`. Habilita o Codex CLI a carregar contexto e invocar skills.

<skills>
### Conformidade com Skills Padrões

- `.claude/rules/code-standards.md` — apenas conteúdo declarativo, manter consistência com CLAUDE.md.
</skills>

<requirements>
- RF2.1 — `AGENTS.md` lista cada skill com forma de invocação Codex (`$kspec-<nome>` ou linguagem natural).
- RF2.2 — `AGENTS.md` referencia rules por caminho (`.agents/rules/<nome>.md`), sem duplicar conteúdo.
- RF2.3 — `AGENTS.md` contém seção "Limitações conhecidas no Codex" cobrindo: ausência de slash commands de projeto, ausência de `AskUserQuestion` em `codex exec`, necessidade de MCP em `.codex/config.toml`.
- RF2.4 — `AGENTS.md` é coerente com `CLAUDE.md` (mesma descrição do projeto, mesmos paths).
- RF3.1 — Cada `.codex/agents/<nome>.toml` contém `name`, `description`, `sandbox_mode` e `developer_instructions`.
- RF3.2 — Mapa de sandbox: `kspec-task-runner` → `workspace-write`, `kspec-review-runner` → `read-only`, `kspec-qa-runner` → `workspace-write`.
- 9 symlinks relativos em `.codex/skills/<nome>` → `../../.agents/skills/<nome>`.
- TOML escapado: `\` → `\\`, `"` → `\"` em `description`; corpo em string `"""..."""` com guard contra ocorrências literais de `"""`.
</requirements>

## Subtarefas

- [ ] 2.1 Escrever `AGENTS.md` raiz com seções: Visão Geral, Estrutura, Skills disponíveis (forma de invocação Codex), Agents, Rules (refs por path), MCP opt-in, Limitações conhecidas no Codex.
- [ ] 2.2 Gerar manualmente `.codex/agents/kspec-task-runner.toml` (sandbox `workspace-write`), `.codex/agents/kspec-review-runner.toml` (sandbox `read-only`), `.codex/agents/kspec-qa-runner.toml` (sandbox `workspace-write`), copiando o frontmatter (`name`, `description`) e o corpo de cada `.agents/agents/<nome>/AGENT.md`.
- [ ] 2.3 Criar 9 symlinks relativos `.codex/skills/<nome>` → `../../.agents/skills/<nome>`.
- [ ] 2.4 Validar smoke: `[ -f AGENTS.md ]`, `grep 'sandbox_mode = "workspace-write"' .codex/agents/kspec-task-runner.toml`, `grep 'sandbox_mode = "read-only"' .codex/agents/kspec-review-runner.toml`.

## Detalhes de Implementação

Ver `techspec.md` §Design de Implementação > Modelos de Dados (frontmatter, TOML, mapa de sandbox) e §Verificações Técnicas > Segurança (regras de escape TOML).

## Critérios de Sucesso

- `AGENTS.md` existe na raiz e atende RF2.1–RF2.4
- 3 `.toml` válidos em `.codex/agents/` com sandbox correto por agent
- 9 symlinks em `.codex/skills/` resolvendo para `.agents/skills/`
- `tar -tvf $(npm pack --dry-run 2>/dev/null | tail -1) | grep -E 'AGENTS.md|.codex/'` deve listar entradas (validado depois em 11.0)

## Testes da Tarefa

- [ ] Smoke shell com os asserts acima
- [ ] Validação manual: `cd <projeto-target> && codex` carrega `AGENTS.md` automaticamente

<critical>SEMPRE CRIE E EXECUTE OS TESTES DA TAREFA ANTES DE CONSIDERÁ-LA FINALIZADA</critical>

## Arquivos relevantes

- `AGENTS.md` (raiz, novo)
- `.codex/agents/kspec-task-runner.toml` (novo)
- `.codex/agents/kspec-review-runner.toml` (novo)
- `.codex/agents/kspec-qa-runner.toml` (novo)
- `.codex/skills/<nome>` (9 symlinks novos)
