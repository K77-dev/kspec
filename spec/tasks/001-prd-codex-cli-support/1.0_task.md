# Tarefa 1.0: Reestruturar `.claude/` real para `.agents/` como source of truth

<critical>Ler os arquivos de prd.md e techspec.md desta pasta, se você não ler esses arquivos sua tarefa será invalidada</critical>

## Dependências

- Nenhuma

## Estimativa

- **Tamanho**: G
- **Horas estimadas**: 4-6h

## Visão Geral

Promover `.agents/` a source of truth físico de skills, agents, rules, templates e validation. Mover o conteúdo real hoje em `.claude/<sub>` para `.agents/<sub>`, reescrever todas as refs `@.claude/...` em SKILL/AGENT para `@.agents/...` e substituir `.claude/<sub>` por symlinks relativos apontando para `.agents/`, preservando `.claude/settings.json` e `.claude/settings.local.json` como arquivos reais.

<skills>
### Conformidade com Skills Padrões

- `.claude/rules/code-standards.md` — não há código novo nesta task, mas preservar nomenclatura kebab-case dos arquivos movidos.
</skills>

<requirements>
- RF1.1 — Substituir as 11 ocorrências de `@.claude/...` em SKILL.md/AGENT.md por `@.agents/...`.
- RF1.2 — Após a reestruturação, `find .agents -type f` retorna todos os arquivos canônicos (≥23: 9 skills + 3 agents + 4 rules + 6 templates + 1 validation).
- RF1.3 — `find .claude -maxdepth 2 -type l` em macOS retorna ≥12 entradas (9 skills + 3 agents) + 3 symlinks de diretório (rules, templates, validation).
- RF1.4 — `.claude/settings.json` e `.claude/settings.local.json` permanecem `isFile()` (nunca symlink).
- Symlinks devem ser **relativos** (`../../.agents/skills/<nome>`), não absolutos.
- `.claude/.enterprise-skills-cache/` permanece intocado.
</requirements>

## Subtarefas

- [ ] 1.1 Criar estrutura `.agents/{skills,agents,rules,templates,validation}/` e mover o conteúdo de `.claude/` correspondente (preservando `settings*.json` e `.enterprise-skills-cache/` na origem).
- [ ] 1.2 Reescrever as 11 refs `@.claude/` → `@.agents/` em `.agents/skills/**/SKILL.md` e `.agents/agents/**/AGENT.md`.
- [ ] 1.3 Criar symlinks relativos por skill/agent: `.claude/skills/<nome>` → `../../.agents/skills/<nome>` e `.claude/agents/<nome>` → `../../.agents/agents/<nome>`.
- [ ] 1.4 Criar symlinks de diretório: `.claude/rules` → `../.agents/rules`, `.claude/templates` → `../.agents/templates`, `.claude/validation` → `../.agents/validation`.
- [ ] 1.5 Validar com smoke shell os 4 asserts dos requisitos RF1.1–RF1.4.

## Detalhes de Implementação

Ver `techspec.md` §Sequenciamento de Desenvolvimento (PR-1) e §Design de Implementação > Modelos de Dados > Estrutura de saída no projeto-alvo. Os símbolos relativos estão definidos lá explicitamente.

## Critérios de Sucesso

- `find .agents -type f | wc -l` ≥ 23
- `find .claude -maxdepth 2 -type l | wc -l` ≥ 12
- `grep -rc '@.agents/' .agents/skills .agents/agents` retorna ≥11; `grep -rc '@.claude/' .agents/skills .agents/agents` retorna 0
- `test -f .claude/settings.json && test -f .claude/settings.local.json` (arquivos reais)
- Cada symlink resolve com `readlink -f`

## Testes da Tarefa

- [ ] Smoke shell (assertivas acima rodando localmente)
- [ ] Validação manual: abrir Claude Code e disparar `/kspec-version` — deve listar 9 skills via symlinks
- [ ] Validação manual: invocar `/kspec-prd test` — deve resolver template `.claude/templates/prd-template.md` via symlink

<critical>SEMPRE CRIE E EXECUTE OS TESTES DA TAREFA ANTES DE CONSIDERÁ-LA FINALIZADA</critical>

## Arquivos relevantes

- `.agents/skills/<nome>/SKILL.md` (9) — movidos
- `.agents/agents/<nome>/AGENT.md` (3) — movidos
- `.agents/rules/*.md` (4) — movidos
- `.agents/templates/*.md` (6) — movidos
- `.agents/validation/enterprise-skills-check.md` (1) — movido
- `.claude/skills/<nome>` (9) — symlinks
- `.claude/agents/<nome>` (3) — symlinks
- `.claude/{rules,templates,validation}` (3) — symlinks de diretório
- `.claude/settings.json`, `.claude/settings.local.json` — preservados
