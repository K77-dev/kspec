# Tarefa 4.0: Setup Vitest e scripts de teste

<critical>Ler os arquivos de prd.md e techspec.md desta pasta, se você não ler esses arquivos sua tarefa será invalidada</critical>

## Dependências

- Nenhuma

## Estimativa

- **Tamanho**: P
- **Horas estimadas**: 1-2h

## Visão Geral

Adicionar Vitest como devDependency e habilitar a execução de testes unitários para as próximas tasks (5.0, 6.0, 7.0, 8.0). Inclui scripts `test` e `test:watch`, configuração do `tsconfig` para incluir `tests/`, e estrutura inicial da pasta `tests/`.

<skills>
### Conformidade com Skills Padrões

- `.claude/rules/code-standards.md` — kebab-case para arquivos.
</skills>

<requirements>
- Vitest adicionado em `devDependencies` (versão estável mais recente compatível com Node ≥ 18).
- Scripts em `package.json`: `"test": "vitest run"`, `"test:watch": "vitest"`.
- `tsconfig.json` inclui `tests/**/*` no `include` (se necessário) sem afetar o build (`tsup` usa `src/`).
- Pasta `tests/` criada com um teste de smoke trivial (`tests/smoke.spec.ts` com `expect(true).toBe(true)`) para validar a pipeline.
- `npm test` retorna exit code 0 e mostra ≥1 teste passando.
</requirements>

## Subtarefas

- [ ] 4.1 `npm install --save-dev vitest` (versão compatível com Node 18+).
- [ ] 4.2 Adicionar scripts `test` e `test:watch` em `package.json`.
- [ ] 4.3 Atualizar `tsconfig.json` para incluir `tests/` se a config base não cobrir (e garantir que o `tsup` continue gerando apenas `src/`).
- [ ] 4.4 Criar `tests/smoke.spec.ts` mínimo para validar a infra.
- [ ] 4.5 Rodar `npm test` e confirmar saída verde.

## Detalhes de Implementação

Ver `techspec.md` §Abordagem de Testes > Testes Unidade (Vitest mencionado como devDep com scripts `test`/`test:watch`).

## Critérios de Sucesso

- `npm test` passa
- `npm test -- --reporter=verbose` lista o teste de smoke
- `npm run build` (tsup) continua funcionando sem regressão
- `package.json` reflete devDep e scripts

## Testes da Tarefa

- [ ] Teste de smoke trivial (`tests/smoke.spec.ts`)
- [ ] Build pós-instalação (`npm run build`) sem erros

<critical>SEMPRE CRIE E EXECUTE OS TESTES DA TAREFA ANTES DE CONSIDERÁ-LA FINALIZADA</critical>

## Arquivos relevantes

- `package.json` (modificado: `devDependencies`, `scripts`)
- `tsconfig.json` (modificado se necessário)
- `tests/smoke.spec.ts` (novo)
