# Tarefa 13.0: Bump versão 1.1.3 → 1.2.0 e `doc/release-checklist.md`

<critical>Ler os arquivos de prd.md e techspec.md desta pasta, se você não ler esses arquivos sua tarefa será invalidada</critical>

## Dependências

- 11.0, 12.0

## Estimativa

- **Tamanho**: P
- **Horas estimadas**: 1-2h

## Visão Geral

Encerrar a release: bump da versão em `VERSION` e `package.json` (1.1.3 → **1.2.0**), atualizar `description` e `keywords` para refletir o suporte Codex, e criar `doc/release-checklist.md` com a sequência de verificações manuais E2E exigidas pela techspec (que não são automatizáveis sem instalar `claude` e `codex` CLIs).

<skills>
### Conformidade com Skills Padrões

- `.claude/rules/code-standards.md` — N/A.
</skills>

<requirements>
- RF6.4 — `package.json` e `VERSION` em **1.2.0**; `description` menciona Codex CLI; `keywords` inclui `codex` e `openai-codex`.
- `doc/release-checklist.md` cobre (da techspec §Testes E2E):
  - Claude Code: `/kspec-version` → lista 9 skills.
  - Claude Code: `/kspec-prd test` → resolve template via symlink.
  - Codex CLI: `cd <projeto> && codex` → carrega `AGENTS.md`.
  - Codex CLI: `$kspec-version` → skill encontrada e executada.
  - Codex CLI: `$kspec-task-runner` → TOML carregado, sandbox aplicado.
  - Smoke shell verde (`npm run smoke`).
  - `npm pack --dry-run` lista `.agents/`, `.codex/`, `AGENTS.md`.
  - `npm view @k77-dev/kspec version` (pós-publicação) = `1.2.0`.
- Bump deve ser feito apenas após Tasks 11.0 e 12.0 (smoke + docs) aprovadas.
</requirements>

## Subtarefas

- [ ] 13.1 Editar `VERSION`: `1.1.3` → `1.2.0`.
- [ ] 13.2 Editar `package.json`: `version` → `1.2.0`; `description` adicionar menção "OpenAI Codex CLI"; `keywords` adicionar `codex` e `openai-codex`.
- [ ] 13.3 Criar `doc/release-checklist.md` com a lista E2E completa da techspec §Testes E2E.
- [ ] 13.4 Validar: `node -p "require('./package.json').version"` retorna `1.2.0`; `cat VERSION` retorna `1.2.0`.
- [ ] 13.5 Rodar `npm run build && npm run smoke && bash scripts/prepublish-check.sh` localmente — todos verdes antes de qualquer `npm publish`.

## Detalhes de Implementação

Ver `techspec.md` §Sequenciamento de Desenvolvimento (PR-3), §Abordagem de Testes > Testes de E2E (checklist manual), §Considerações Técnicas > Decisões Principais.

## Critérios de Sucesso

- `VERSION` e `package.json#version` = `1.2.0` (consistentes)
- `doc/release-checklist.md` exists e cobre os 8 itens acima
- Build + smoke + prepublish-check passam localmente
- Sem alteração funcional no código (só metadata e doc)

## Testes da Tarefa

- [ ] Verificação manual da versão em ambos os arquivos
- [ ] Build + smoke + prepublish-check rodados localmente
- [ ] Revisão manual do checklist (cobre todos os fluxos de E2E)

<critical>SEMPRE CRIE E EXECUTE OS TESTES DA TAREFA ANTES DE CONSIDERÁ-LA FINALIZADA</critical>

## Arquivos relevantes

- `VERSION` (modificado)
- `package.json` (modificado: `version`, `description`, `keywords`)
- `doc/release-checklist.md` (novo)
