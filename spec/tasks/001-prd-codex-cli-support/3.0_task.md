# Tarefa 3.0: Atualizar `CLAUDE.md` e SKILL `kspec-version` para nova arquitetura

<critical>Ler os arquivos de prd.md e techspec.md desta pasta, se você não ler esses arquivos sua tarefa será invalidada</critical>

## Dependências

- 1.0

## Estimativa

- **Tamanho**: P
- **Horas estimadas**: 1-2h

## Visão Geral

Sincronizar a documentação interna com a nova arquitetura: `CLAUDE.md` passa a refletir `.agents/` como source of truth e a presença de `AGENTS.md`/`.codex/`. A SKILL `kspec-version` passa a varrer `.agents/skills/*/SKILL.md` (em vez de `.claude/skills/`) e imprimir "Plataformas suportadas: Claude Code, OpenAI Codex CLI".

<skills>
### Conformidade com Skills Padrões

- N/A — task de conteúdo declarativo.
</skills>

<requirements>
- `CLAUDE.md` descreve `.agents/` como source of truth, lista `.codex/` e `AGENTS.md`, mantém estrutura atual de seções.
- RF6.3 — `kspec-version` agrega `.agents/skills/*/SKILL.md` (fonte canônica) e imprime "Plataformas suportadas: Claude Code, OpenAI Codex CLI".
- Sem mudanças no formato da saída do `kspec-version` além das duas linhas acima (preservar UX).
</requirements>

## Subtarefas

- [ ] 3.1 Editar `CLAUDE.md`: atualizar seção "Estrutura do projeto" para refletir `.agents/` + `.codex/` + `AGENTS.md`; adicionar nota explícita "`.agents/` é source of truth, `.claude/` e `.codex/` são camadas de discovery".
- [ ] 3.2 Editar `.agents/skills/kspec-version/SKILL.md`: substituir varredura `.claude/skills/` por `.agents/skills/`; adicionar linha "Plataformas suportadas: Claude Code, OpenAI Codex CLI".
- [ ] 3.3 Validar: `grep -q ".agents/" CLAUDE.md` e `grep -q "Plataformas suportadas" .agents/skills/kspec-version/SKILL.md`.

## Detalhes de Implementação

Ver `techspec.md` §Arquivos relevantes e dependentes (lista CLAUDE.md e `.agents/skills/kspec-version/SKILL.md` entre os modificados).

## Critérios de Sucesso

- `CLAUDE.md` cita `.agents/`, `.codex/` e `AGENTS.md`
- `/kspec-version` (Claude Code) lista 9 skills lidas de `.agents/skills/`
- Saída inclui "Plataformas suportadas: Claude Code, OpenAI Codex CLI"

## Testes da Tarefa

- [ ] Validação manual: executar `/kspec-version` no Claude Code e conferir saída

<critical>SEMPRE CRIE E EXECUTE OS TESTES DA TAREFA ANTES DE CONSIDERÁ-LA FINALIZADA</critical>

## Arquivos relevantes

- `CLAUDE.md` (modificado)
- `.agents/skills/kspec-version/SKILL.md` (modificado)
