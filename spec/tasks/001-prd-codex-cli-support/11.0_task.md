# Tarefa 11.0: Atualizar `package.json#files` e `README.md` com matriz e limitações

<critical>Ler os arquivos de prd.md e techspec.md desta pasta, se você não ler esses arquivos sua tarefa será invalidada</critical>

## Dependências

- 2.0, 9.0

## Estimativa

- **Tamanho**: M
- **Horas estimadas**: 2-3h

## Visão Geral

Garantir que o tarball publicado inclui os novos artefatos (`.agents/`, `.codex/`, `AGENTS.md`) e documentar a release no `README.md`: matriz "plataforma → onde vivem skills/agents/rules/templates → como invocar", e a seção de limitações conhecidas (`codex exec`, slash commands, Windows, MCP, sandbox).

<skills>
### Conformidade com Skills Padrões

- `.claude/rules/code-standards.md` — N/A (conteúdo declarativo).
</skills>

<requirements>
- RF4.5 — `package.json#files` inclui `.agents/`, `.claude/`, `.codex/`, `AGENTS.md`, `VERSION`, `README.md`.
- RF6.1 — README contém "Matriz de plataformas" indicando, por plataforma, onde vivem skills/agents/rules/templates e como invocar.
- RF6.2 — README e AGENTS.md listam as limitações conhecidas (`codex exec`, slash commands, Windows, MCP, sandbox).
- README continua coerente com `CLAUDE.md` e `AGENTS.md` (mesma descrição, mesmos paths).
</requirements>

## Subtarefas

- [ ] 11.1 Editar `package.json#files`: adicionar `.agents/`, `.codex/`, `AGENTS.md`. Conferir que `.claude/`, `VERSION`, `README.md` já estão.
- [ ] 11.2 Editar `README.md`: adicionar seção "Matriz de plataformas" (tabela com colunas Claude Code / Codex CLI / Caminho canônico).
- [ ] 11.3 Editar `README.md`: adicionar seção "Limitações conhecidas" cobrindo `codex exec`, slash commands, Windows, MCP, sandbox.
- [ ] 11.4 Conferir que `AGENTS.md` também tem a seção de limitações (deve estar feita na Task 2.0; aqui apenas validar coerência).
- [ ] 11.5 Smoke: `npm pack --dry-run | grep -E '.agents/|.codex/|AGENTS.md'` retorna entradas.

## Detalhes de Implementação

Ver `techspec.md` §Arquivos relevantes e dependentes (lista `package.json`, `README.md`, `AGENTS.md` entre modificados) e PRD §F6 (Documentação e versionamento).

## Critérios de Sucesso

- `package.json#files` inclui os 3 novos itens
- `npm pack --dry-run` lista `.agents/`, `.codex/`, `AGENTS.md`
- README contém matriz e limitações
- Sem divergência entre README, CLAUDE.md e AGENTS.md

## Testes da Tarefa

- [ ] `npm pack --dry-run` (asserts acima)
- [ ] Revisão manual de coerência entre README/CLAUDE.md/AGENTS.md

<critical>SEMPRE CRIE E EXECUTE OS TESTES DA TAREFA ANTES DE CONSIDERÁ-LA FINALIZADA</critical>

## Arquivos relevantes

- `package.json` (modificado)
- `README.md` (modificado)
- `AGENTS.md` (validado)
